<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Truyencuoi2">

        <!-- Màn hình Splash (khởi động đầu tiên) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Màn hình chính (danh sách chủ đề) -->
        <activity android:name=".MainActivity" />

        <!-- Màn hình danh sách truyện -->
        <activity android:name=".StoryListActivity" />

        <!-- Màn hình chi tiết truyện -->
        <activity android:name=".StoryDetailActivity" />

    </application>

</manifest>